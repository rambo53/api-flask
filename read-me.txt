- pip install Flask

Pour démarrer mon projet Flask.

- pip install Flask-JSON

Pour faciliter mes échanges JSON avec flask.

- pip install -U flask-cors

Pour authoriser le cors sur les requêtes de mon API, ajoute dans mon header "allow..."

Ensuite je démarre mon application, par défaut le port utilisé est "http://127.0.0.1:5000".